apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: ecommerce
  labels:
    app: inventory-service
    managed-by: argocd
spec:
  type: ClusterIP
    # internal only — only api-gateway talks to this
    # NOT exposed to internet directly
  selector:
    app: inventory-service # routes traffic to pods with this label
  ports:
  - name: http
    port: 8082 # port other services use to call this service
    targetPort: 8082 # port the container listens on
    protocol: TCP
